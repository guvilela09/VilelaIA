[build]
  base = "."
  command = "pnpm install && pnpm --filter vilelaia-web run build"
  publish = "artifacts/web-agent/dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
